/**
 * 旧 el-select helper 的 re-export shim（2026-07 T19 统一后保留）。
 *
 * <p>实际实现已迁移至 `el-control.ts`，本文件仅作 import 兼容层。
 * 新代码请直接 import 自 `helpers/el-control`。
 *
 * @see `./el-control` 统一实现
 */
export { selectElOption } from './el-control';
