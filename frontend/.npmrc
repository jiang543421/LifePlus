# 强制扁平 node_modules（与 npm/yarn 一致）。
# 原因：vue-tsc 2.x 的 bin 路径在 dist/cli.js，pnpm 9 的 symlink 解析会
# 命中老的 bin/vue-tsc.js 触发 "Cannot find module"。
# 单 package 项目 hoist 无副作用；多 workspace 慎用。
node-linker=hoisted