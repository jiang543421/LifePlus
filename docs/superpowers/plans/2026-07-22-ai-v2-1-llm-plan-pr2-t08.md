### Task 8: LlmPromptBuilder + llm-prompt.properties

**Files:**
- Create: `backend/src/main/java/com/lifepulse/ai/llm/LlmPromptBuilder.java`
- Create: `backend/src/main/resources/llm-prompt.properties`
- Create: `backend/src/test/java/com/lifepulse/ai/llm/LlmPromptBuilderTest.java`

- [ ] **Step 1: Write failing test**

`LlmPromptBuilderTest.java`:

```java
package com.lifepulse.ai.llm;

import com.lifepulse.ai.model.MetricValue;
import org.junit.jupiter.api.Test;
import java.time.LocalDate;
import java.util.List;
import static org.assertj.core.api.Assertions.assertThat;

class LlmPromptBuilderTest {

    private final LlmPromptBuilder builder = new LlmPromptBuilder();

    @Test
    void build_allChipsPresent_returnsRequestWithUserPrompt() {
        List<MetricValue> metrics = List.of(
            new MetricValue(new java.math.BigDecimal("80"), com.lifepulse.ai.model.Trend.UP, "+5pp"),
            new MetricValue(new java.math.BigDecimal("420"), com.lifepulse.ai.model.Trend.DOWN, "-12%"),
            new MetricValue(new java.math.BigDecimal("4"), com.lifepulse.ai.model.Trend.FLAT, "持平")
        );
        LlmRequest req = builder.build(1L, metrics, LocalDate.of(2026, 7, 22));
        assertThat(req.systemPrompt()).contains("个人数字生活助手");
        assertThat(req.userPrompt()).contains("【任务】完成率 80%");
        assertThat(req.userPrompt()).contains("【消费】本周 ¥420");
        assertThat(req.userPrompt()).contains("【日程】今日 4 项");
        assertThat(req.maxResponseTokens()).isPositive();
    }

    @Test
    void build_emptyMetrics_rendersSkeleton() {
        LlmRequest req = builder.build(1L, List.of(), LocalDate.of(2026, 7, 22));
        assertThat(req.userPrompt()).contains("暂无数据");
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && ./mvnw test -Dtest=LlmPromptBuilderTest`
Expected: FAIL

- [ ] **Step 3: Write minimal implementation**

`LlmPromptBuilder.java`:

```java
package com.lifepulse.ai.llm;

import com.lifepulse.ai.AiConstants;
import com.lifepulse.ai.model.MetricValue;
import org.springframework.core.io.ClassPathResource;
import org.springframework.stereotype.Component;
import java.io.IOException;
import java.io.InputStreamReader;
import java.nio.charset.StandardCharsets;
import java.time.Duration;
import java.time.LocalDate;
import java.util.List;
import java.util.Properties;

@Component
public class LlmPromptBuilder {

    private final Properties props = loadProps();

    public LlmRequest build(long userId, List<MetricValue> metrics, LocalDate today) {
        String system = props.getProperty("system.role", "").trim();
        String user = renderUserPrompt(metrics, today);
        int maxTokens = parseInt(props.getProperty("max.response.tokens", "300"));
        Duration timeout = Duration.ofMillis(parseInt(props.getProperty("timeout.ms", "5000")));
        return new LlmRequest(system, user, maxTokens, timeout);
    }

    private String renderUserPrompt(List<MetricValue> metrics, LocalDate today) {
        if (metrics.isEmpty()) {
            return props.getProperty("user.empty", "暂无数据");
        }
        StringBuilder sb = new StringBuilder();
        for (MetricValue mv : metrics) {
            String key = mapKey(mv);
            sb.append(props.getProperty("user.chip." + key, "")).append("\n");
        }
        String template = props.getProperty("user.template", "{0}");
        return template.replace("{0}", sb.toString())
            .replace("{date}", today.toString());
    }

    private String mapKey(MetricValue mv) {
        // metric value 类型不可用，简化：用 value 非空 + trend 推测
        // 实际接入时由 Service 按 provider key 排序传入
        if (mv.value() == null) return "empty";
        return "taskCompletion";
    }

    private static int parseInt(String s) {
        try { return Integer.parseInt(s.trim()); } catch (NumberFormatException e) { return 0; }
    }

    private static Properties loadProps() {
        Properties p = new Properties();
        try (var in = new InputStreamReader(
                new ClassPathResource("llm-prompt.properties").getInputStream(),
                StandardCharsets.UTF_8)) {
            p.load(in);
        } catch (IOException ex) {
            throw new IllegalStateException("Failed to load llm-prompt.properties", ex);
        }
        return p;
    }
}
```

> **注意**：上面的 `mapKey` 是简化实现，Task 8 完成后会被 Task 14 Service 传入 `Map<String, MetricValue>` 形式覆盖。本 task 只验证"渲染 pipeline 工作"。

`backend/src/main/resources/llm-prompt.properties`:

```properties
# LLM prompt template (CLAUDE.md §11 / spec §9)
system.role=你是一个个人数字生活助手。基于用户今日的 4 个维度数据，生成简洁的中文洞察和建议。要求：1) 只基于提供的数据，不要编造；2) 中文，1-2 句；3) 不超过 120 字；4) 包含 1 个判断（紧凑/略松/正常/异常）和 1 个数字；5) 语气像朋友建议。

# User prompt template
user.template=请基于以下数据生成洞察（JSON 格式，4 字段：headline, advice, highlight, mood）：

{0}

要求：headline 40-120 字；advice 30-80 字；highlight 20-60 字；mood ∈ positive/neutral/cautious。
user.empty=暂无数据。

# User chip rows (each is {key}={value},{delta-text})
user.chip.taskCompletion=【任务】完成率 {0}%，{1}
user.chip.weeklyExpense=【消费】本周 ¥{0}，{1}
user.chip.planDensity=【日程】今日 {0} 项，{1}
user.chip.dietIntake=【饮食】摄入 {0}/{1} kcal

# Client params
max.response.tokens=300
timeout.ms=5000
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && ./mvnw test -Dtest=LlmPromptBuilderTest`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/java/com/lifepulse/ai/llm/LlmPromptBuilder.java
git add backend/src/main/resources/llm-prompt.properties
git add backend/src/test/java/com/lifepulse/ai/llm/LlmPromptBuilderTest.java
git commit -m "feat(ai-llm): add LlmPromptBuilder with prompt template"
```

---
