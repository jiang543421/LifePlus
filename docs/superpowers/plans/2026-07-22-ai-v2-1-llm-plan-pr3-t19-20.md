## PR3 — 独立分析页 + 前端（7 个 task，估算 2-3 天）

### Task 19: AiInsightController 加 GET /analysis endpoint

**Files:**
- Modify: `backend/src/main/java/com/lifepulse/ai/web/AiInsightController.java`

- [ ] **Step 1: Write failing test**

在 `AiInsightControllerWebTest.java` 追加：

```java
@Test
void getAnalysis_validToken_returns200WithLlmMeta() throws Exception {
    when(service.getInsight(anyLong())).thenReturn(sampleResponse);

    mvc.perform(get("/api/v1/ai/insight/analysis").header("Authorization", "Bearer valid"))
        .andExpect(status().isOk())
        .andExpect(jsonPath("$.data.headline").value("h"))
        .andExpect(jsonPath("$.data.source").value("llm"));
}

@Test
void getAnalysis_noAuth_returns401() throws Exception {
    mvc.perform(get("/api/v1/ai/insight/analysis"))
        .andExpect(status().isUnauthorized());
}

@Test
void getAnalysis_rateLimitExceeded_returns1006() throws Exception {
    when(rateLimiter.hit(anyString(), anyInt(), any(Duration.class))).thenReturn(true);

    mvc.perform(get("/api/v1/ai/insight/analysis").header("Authorization", "Bearer valid"))
        .andExpect(status().isOk())  // 业务异常由 GlobalExceptionHandler 转 429
        .andExpect(jsonPath("$.code").value(1006));
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && ./mvnw test -Dtest=AiInsightControllerWebTest`
Expected: FAIL

- [ ] **Step 3: Modify controller**

在 `AiInsightController.java` 中 `refresh()` 后追加：

```java
/**
 * 返回洞察（独立分析页专用，与 /today 共享缓存）。
 * 限流与 /today 同：30/min/user。
 */
@GetMapping("/analysis")
public MyResponse<AiInsightResponse> analysis(@AuthenticationPrincipal Long userId) {
    requireUserId(userId);
    enforceRateLimit(userId, AiConstants.INSIGHT_GET_RL_MAX, AiConstants.INSIGHT_GET_RL_WINDOW);
    AiInsightResponse r = service.getInsight(userId);
    return MyResponse.ok(withFreshness(r));
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && ./mvnw test -Dtest=AiInsightControllerWebTest`
Expected: PASS（新增 3 用例）

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/java/com/lifepulse/ai/web/AiInsightController.java
git add backend/src/test/java/com/lifepulse/ai/web/AiInsightControllerWebTest.java
git commit -m "feat(ai-llm): add GET /api/v1/ai/insight/analysis endpoint"
```

---

### Task 20: 前端 types/ai.ts 扩展

**Files:**
- Modify: `frontend/src/types/ai.ts`

- [ ] **Step 1: Modify file**

```typescript
export type Trend = 'UP' | 'DOWN' | 'FLAT' | 'NONE';
export type AiSource = 'llm' | 'template';
export type Mood = 'POSITIVE' | 'NEUTRAL' | 'CAUTIOUS';

export interface AiChip {
  key: string;
  label: string;
  value: string;
  unit: string;
  trend: Trend;
  deltaText: string;
}

export interface LlmMeta {
  promptTokens: number;
  responseTokens: number;
  latencyMs: number;
}

export interface AiInsight {
  headline: string;
  chips: AiChip[];
  generatedAt: string;       // ISO-8601
  freshnessSeconds: number;
  // v2.1 新增字段（v2.0 旧条目无 source/advice 等时为 undefined）
  source?: AiSource;
  advice?: string;
  highlight?: string;
  mood?: Mood;
  llmMeta?: LlmMeta;
}

export interface AiRefreshResponse {
  success: boolean;
  data: AiInsight;
}
```

- [ ] **Step 2: 现有前端测试**

Run: `cd frontend && pnpm test -- --reporter=verbose`
Expected: PASS（无 v2.0 旧测试断言）

- [ ] **Step 3: Commit**

```bash
git add frontend/src/types/ai.ts
git commit -m "feat(ai-llm): extend frontend AiInsight type with v2.1 fields"
```

---
