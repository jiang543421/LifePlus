### Task 13: ErrorCode 追加 1510-1513

**Files:**
- Modify: `backend/src/main/java/com/lifepulse/common/exception/ErrorCode.java`

- [ ] **Step 1: Write failing test**

`backend/src/test/java/com/lifepulse/common/exception/ErrorCodeLLMTest.java`:

```java
package com.lifepulse.common.exception;

import org.junit.jupiter.api.Test;
import static org.assertj.core.api.Assertions.assertThat;

class ErrorCodeLLMTest {

    @Test
    void llmErrorCodes_areInRange1510to1513() {
        assertThat(ErrorCode.LLM_QUOTA_EXCEEDED).isEqualTo(1510);
        assertThat(ErrorCode.LLM_CIRCUIT_OPEN).isEqualTo(1511);
        assertThat(ErrorCode.LLM_RESPONSE_INVALID).isEqualTo(1512);
        assertThat(ErrorCode.LLM_UNAVAILABLE).isEqualTo(1513);
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && ./mvnw test -Dtest=ErrorCodeLLMTest`
Expected: FAIL (constants not defined)

- [ ] **Step 3: Modify `ErrorCode.java`**

在 `AI_DEGRADED = 1501` 后面追加：

```java
    /** 1510：LLM 每日配额超限（spec §6.3）。Service 内部 catch 走 L2 模板，不直接返用户。 */
    public static final int LLM_QUOTA_EXCEEDED = 1510;

    /** 1511：LLM 熔断中（spec §6.4）。Service 内部 catch 走 L2 模板，不直接返用户。 */
    public static final int LLM_CIRCUIT_OPEN = 1511;

    /** 1512：LLM 响应解析失败（spec §5.6）。Service 内部 catch 走 L2 模板，不直接返用户。 */
    public static final int LLM_RESPONSE_INVALID = 1512;

    /** 1513：LLM 不可用（5xx/429/超时/网络错误）。Service 内部 catch 走 L2 模板。 */
    public static final int LLM_UNAVAILABLE = 1513;
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && ./mvnw test -Dtest=ErrorCodeLLMTest`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/java/com/lifepulse/common/exception/ErrorCode.java
git add backend/src/test/java/com/lifepulse/common/exception/ErrorCodeLLMTest.java
git commit -m "feat(exception): add LLM error codes 1510-1513 (1510 quota, 1511 circuit, 1512 invalid, 1513 unavailable)"
```

---
