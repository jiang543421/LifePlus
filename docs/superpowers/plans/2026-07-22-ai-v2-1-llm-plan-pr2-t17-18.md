### Task 18: PR2 全套验证

**Files:** 无新增

- [ ] **Step 1: 全套单测 + IT 通过**

Run: `cd backend && ./mvnw verify`
Expected: BUILD SUCCESS，JaCoCo 报告：
- `LlmInsightGenerator`: ≥ 90%
- `LlmCircuitBreaker`: ≥ 85%
- `LlmQuotaGuard`: ≥ 85%
- `LlmJsonParser`: ≥ 80%
- `DeepSeekClient`: ≥ 80%
- `OllamaClient`: ≥ 80%
- `AiInsightService`: ≥ 85%

- [ ] **Step 2: 启动验证：cache TTL 6h**

Run:
```bash
cd backend && ./mvnw spring-boot:run
# 登录获取 token
curl -X POST http://localhost:8080/api/v1/auth/login -d '{"email":"x@y.com","password":"xxx"}' -H "Content-Type: application/json"
# 触发 insight
curl http://localhost:8080/api/v1/ai/insight/today -H "Authorization: Bearer <token>"
# 检查 Redis TTL
redis-cli TTL "lp:ai:insight:1"
```
Expected: TTL 在 (21599, 21600) 之间

- [ ] **Step 3: PR2 merge**

```bash
git checkout main && git merge --no-ff feat/ai-llm-v2.1 -m "merge PR2 LLM business orchestration with fallback chain"
```

---
