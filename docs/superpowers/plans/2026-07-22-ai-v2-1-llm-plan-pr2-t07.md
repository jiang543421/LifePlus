## PR2 — 业务编排 + 降级链（10 个 task，估算 3-5 天）

### Task 7: LlmInsightPayload + Mood + LlmMeta

**Files:**
- Create: `backend/src/main/java/com/lifepulse/ai/llm/LlmInsightPayload.java`
- Create: `backend/src/main/java/com/lifepulse/ai/llm/Mood.java`
- Create: `backend/src/main/java/com/lifepulse/ai/llm/LlmMeta.java`

- [ ] **Step 1: Write failing test**

`backend/src/test/java/com/lifepulse/ai/llm/LlmInsightPayloadTest.java`:

```java
package com.lifepulse.ai.llm;

import org.junit.jupiter.api.Test;
import static org.assertj.core.api.Assertions.assertThat;

class LlmInsightPayloadTest {

    @Test
    void mood_fromString_clampsUnknownToNeutral() {
        assertThat(Mood.fromString("positive")).isEqualTo(Mood.POSITIVE);
        assertThat(Mood.fromString("NEUTRAL")).isEqualTo(Mood.NEUTRAL);
        assertThat(Mood.fromString("cautious")).isEqualTo(Mood.CAUTIOUS);
        assertThat(Mood.fromString("weird_value")).isEqualTo(Mood.NEUTRAL);
        assertThat(Mood.fromString(null)).isEqualTo(Mood.NEUTRAL);
    }

    @Test
    void payload_recordHoldsAllFields() {
        var meta = new LlmMeta(10, 20, 1500L);
        var p = new LlmInsightPayload("h", "a", "hi", Mood.POSITIVE, meta);
        assertThat(p.headline()).isEqualTo("h");
        assertThat(p.mood()).isEqualTo(Mood.POSITIVE);
        assertThat(p.llmMeta().latencyMs()).isEqualTo(1500L);
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && ./mvnw test -Dtest=LlmInsightPayloadTest`
Expected: FAIL (classes not found)

- [ ] **Step 3: Write minimal implementation**

`LlmInsightPayload.java`:

```java
package com.lifepulse.ai.llm;

public record LlmInsightPayload(
    String headline,
    String advice,
    String highlight,
    Mood mood,
    LlmMeta llmMeta
) {
    public LlmInsightPayload(String headline, String advice, String highlight, Mood mood,
                             int promptTokens, int responseTokens, long latencyMs) {
        this(headline, advice, highlight, mood,
            new LlmMeta(promptTokens, responseTokens, latencyMs));
    }
}
```

`Mood.java`:

```java
package com.lifepulse.ai.llm;

import com.fasterxml.jackson.annotation.JsonCreator;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public enum Mood {
    POSITIVE, NEUTRAL, CAUTIOUS;

    private static final Logger log = LoggerFactory.getLogger(Mood.class);

    @JsonCreator
    public static Mood fromString(String s) {
        if (s == null) return NEUTRAL;
        return switch (s.toLowerCase()) {
            case "positive" -> POSITIVE;
            case "neutral"  -> NEUTRAL;
            case "cautious" -> CAUTIOUS;
            default -> {
                log.warn("Mood: unknown value '{}', clamped to NEUTRAL", s);
                yield NEUTRAL;
            }
        };
    }
}
```

`LlmMeta.java`:

```java
package com.lifepulse.ai.llm;

public record LlmMeta(
    int promptTokens,
    int responseTokens,
    long latencyMs
) {}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && ./mvnw test -Dtest=LlmInsightPayloadTest`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/java/com/lifepulse/ai/llm/LlmInsightPayload.java
git add backend/src/main/java/com/lifepulse/ai/llm/Mood.java
git add backend/src/main/java/com/lifepulse/ai/llm/LlmMeta.java
git add backend/src/test/java/com/lifepulse/ai/llm/LlmInsightPayloadTest.java
git commit -m "feat(ai-llm): add LlmInsightPayload, Mood enum, LlmMeta record"
```

---
