### Task 25: 前端守卫：未登录拦截 `/ai-analysis`

**Files:**
- Modify: `frontend/src/router/index.ts`（前一个 task 已加 meta，确保 `beforeEach` 检查 meta.requiresAuth）
- Create: `frontend/src/router/__tests__/ai-analysis-guard.spec.ts`

**Interfaces:**
- Consumes: 既有 `auth-guard.spec.ts` 测试模式
- Produces: 路由守卫对 `/ai-analysis` 跳转生效

- [ ] **Step 1: 确认现有守卫逻辑覆盖 meta.requiresAuth**

Run: `grep -A 20 "beforeEach" frontend/src/router/index.ts`
Expected: 既有守卫已检查 `meta.requiresAuth`，本 task 主要新增专门的守卫测试

- [ ] **Step 2: 添加专门的 ai-analysis 守卫测试**

`frontend/src/router/__tests__/ai-analysis-guard.spec.ts`:

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { createRouter, createMemoryHistory } from 'vue-router';
import { createPinia, setActivePinia } from 'pinia';
import { useAuthStore } from '@/stores/auth';

describe('/ai-analysis route guard', () => {
  beforeEach(() => { setActivePinia(createPinia()); localStorage.clear(); });

  it('redirects to /login when not authenticated', async () => {
    const auth = useAuthStore();
    auth.token = null;
    const router = createRouter({
      history: createMemoryHistory(),
      routes: [
        { path: '/login', name: 'login', component: { template: '<div/>' } },
        { path: '/ai-analysis', name: 'ai-analysis', component: { template: '<div/>' }, meta: { requiresAuth: true } },
      ],
    });
    router.beforeEach((to) => {
      if (to.meta.requiresAuth && !auth.token) return { name: 'login' };
    });
    await router.push('/ai-analysis');
    expect(router.currentRoute.value.name).toBe('login');
  });
});
```

- [ ] **Step 3: Run test**

Run: `cd frontend && pnpm test ai-analysis-guard`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add frontend/src/router/__tests__/ai-analysis-guard.spec.ts
git commit -m "test(ai-llm): assert /ai-analysis requires auth"
```

---

### Task 26: PR3 验证（全量前端 + 类型 + lint）

**Files:**
- Run: `cd frontend && pnpm install`（首次）
- Run: `pnpm test`
- Run: `pnpm exec vue-tsc --noEmit`
- Run: `pnpm exec eslint src --ext .ts,.vue`

- [ ] **Step 1: 安装依赖**

Run: `cd frontend && pnpm install`
Expected: no errors

- [ ] **Step 2: 运行所有单元测试**

Run: `pnpm test`
Expected: ALL PASS（覆盖 store + 4 components + 2 router guards）

- [ ] **Step 3: 类型检查**

Run: `pnpm exec vue-tsc --noEmit`
Expected: 0 errors

- [ ] **Step 4: Lint**

Run: `pnpm exec eslint src --ext .ts,.vue`
Expected: 0 errors

- [ ] **Step 5: 构建**

Run: `pnpm build`
Expected: dist 产出，无 TS 错误

- [ ] **Step 6: 提交空检查 commit（如需要）**

若有任何 format 修复：
```bash
git add -A
git commit -m "chore(ai-llm): PR3 lint + format fixes"
```

---

### Task 27: PR3 合并到 main（squash merge 标题）

> 在 PR UI 界面完成；本 task 是执行清单，不写代码。

- [ ] **Step 1: 推送分支**

```bash
git push origin feat/ai-llm-v2.1
```

- [ ] **Step 2: 创建 PR（标题：`feat(ai-llm): PR3 independent /ai-analysis page`）**

PR 正文需含：
- 改了什么（AiAnalysisView.vue + 路由 + 守卫测试）
- 为什么（CLAUDE.md §11 拆分独立页面的要求）
- 测试覆盖（4 组件测试 + 1 守卫测试 = 5 case）
- 影响面（仅前端，无后端 / DB 改动）
- 是否 break change（无，向后兼容）

- [ ] **Step 3: 等待 CI 通过 + review**

- [ ] **Step 4: Squash merge**

---
